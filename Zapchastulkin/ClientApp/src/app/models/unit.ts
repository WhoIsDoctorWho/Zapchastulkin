import { Product } from "./product";

export class Unit {
    public id?: number;
    public categoryId ?: number;
    public name ?: string;
    public imageUrl ?: string;
    public products ?: Product[];
}